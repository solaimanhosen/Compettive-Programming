#include <stdio.h>

int main()
{
    char ch[2500];
    char st[25];
    int i,j,l;
    while(gets(ch))
    {
        for(i=0;ch[i]!='\0';i++)
        {
            for(j=i;ch[j]!=' ';j++)
            {
                st[j]=ch[]
            }
        }
    }
}
